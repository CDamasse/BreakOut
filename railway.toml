[build]
buildCommand = "composer install --optimize-autoloader --no-dev --no-interaction && npm ci && npm run prod && php artisan config:cache && php artisan route:cache && php artisan view:cache"

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/"
